Youwang's homepage.
===================

Publication data
----------------

Edit `script/publications-data.js` to add or update publications. The shared
records are rendered as homepage cards by `script/publications.js` and as CV
journal/conference citations by `cv/cv-render.js`.
